package interviewpurpose;

public class Charoprations {

	    public static void main(String[] args) {
	      
	//ch is digit
	/*  char ch = '7';

	        if (Character.isDigit(ch)) {
	            System.out.println(ch + " is a digit.");
	        } else {
	            System.out.println(ch + " is not a digit.");
	        }   */

	  // ch is letter alpa  
	 /* char ch = 'A';

	        if (Character.isLetter(ch)) {
	            System.out.println(ch + " is a letter.");
	        } else {
	            System.out.println(ch + " is not a letter.");
	        }*/

	//Converting ch case
	/*char ch = 'a';

	        //Convert to uppercase
	        char upper = Character.toUpperCase(ch);
	        System.out.println("Uppercase of " + ch + " is " + upper);

	        // Convert to lowercase
	        char lower = Character.toLowerCase(ch);
	        System.out.println("Lowercase of " + ch + " is " + lower);
	*/

	// whitespace character
	/*char ch = ' ';

	        if (Character.isWhitespace(ch)) {
	            System.out.println(ch + " is a whitespace character.");
	        } else {
	            System.out.println(ch + " is not a whitespace character.");
	        }*/


	// Character Equality
	/*     char ch1 = 'A';
	        char ch2 = 'a';

	        if (Character.toLowerCase(ch1) == Character.toLowerCase(ch2)) {
	            System.out.println(ch1 + " and " + ch2 + " are equal ignoring case.");
	        } else {
	            System.out.println(ch1 + " and " + ch2 + " are not equal ignoring case.");
	        }*/


	}
	}

	

